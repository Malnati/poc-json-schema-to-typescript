// src/<%= name %>Create.tsx

import React from 'react';
import { Create, SimpleForm, <%=
    Array.from(new Set(fields.map(field => field.component))).join(', ')
%> } from 'react-admin';

const <%= name %>Create: React.FC = () => (
    <Create>
        <SimpleForm>
            <% fields.forEach(field => { %>
                <<%= field.component %> source="<%= field.name %>" />
            <% }) %>
        </SimpleForm>
    </Create>
);

export default <%= name %>Create;